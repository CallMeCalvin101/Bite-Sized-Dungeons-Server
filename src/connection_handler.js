// Maybe a global state for storing all current sessions?


// Hook for handling room join request
// Has to create a room if it doesn't exist

// Hook for handling reconnect

export const connect = (sessionID) => {
}
